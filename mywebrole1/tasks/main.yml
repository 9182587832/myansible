---
# tasks file for wywebrole1
- name: install webserver
  apt:
   name: apache2
   state: present
- name: copy indexfile
  template: 
      src: template/index.j2
      dest: /var/www/html/index.html
  notify: restatredwebserver     